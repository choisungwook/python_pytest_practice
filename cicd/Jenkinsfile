pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'ls'
                sh 'docker run --rm python:3.9.1 \"sleep 3; ls\""'
                sh 'done'
            }
        }
    }
}